1. what is favicon?
    -it is the image in the top left of the tab

2. what is the use of public folder?
    - it will store all the image files, music files or static files.

3. package.json?
    - Very similar to requirements.txt
    - with versions

4. package-lock.json?
    - It is automatically generated for any operations where npm modifies either the node_modules tree or package.json.
    - It describes the exact tree that was generated, such that subsequent installs are able to generate identical trees, regardless of intermediate dependency updates.